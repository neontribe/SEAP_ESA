<h2 class="no-print">Your assessment checklist</h2>
<div class="no-print">
  <h2>Would you qualify?</h2>
  <div class="{{#if answeredOne}}message{{/if}} no-print">
    {{#if answeredOne}} {{#if incomplete}}
    <p style="margin-top: 0">You have answered {{accuracy}} of the questions available. <a href="javascript:void(0)" role="button" data-action="pick">Try some more questions</a> to give you a more accurate idea of whether or not you would qualify.</p>{{/if}} {{else}}
    <p><strong><a href="javascript:void(0)" data-action="start-or-resume">Try some questions <span aria-hidden="true">&#x2192;</span></a></strong></p>
    {{/if}}
  </div>
  {{#if answeredOne}} {{{qualifyHigh}}} {{{qualifyLow}}} {{{qualifyEither}}} {{{qualifyNone}}} {{else}}
  <p>You need to <a href="javascript:void(0)" data-action="start-or-resume">answer some questions</a> so we can get a more accurate idea of whether or not you would qualify.</p>
  {{/if}}
  <h2>Important answers</h2> {{#if supportAnswers}}
  <p>These are the answers you gave that count towards qualification. You should mention them in your assessment.</p>
  <ul class="q-and-a">
    {{#each supportAnswers}}
    <li>
      <p class="warn">Very Important</p>
      <p><strong>Question:</strong> {{question}}</p>
      <p><strong>Your answer:</strong> {{answer}}</p>
      <p class="right no-print"><a href="javascript:void(0)" role="button" data-action="change" data-question="question-{{sluggify question}}">Change your answer?</a></p>
    </li>
    {{/each}}
  </ul>
  {{/if}} {{#if WRAGAnswers}} {{#unless supportAnswers}}
  <p>These are the answers you gave that count towards qualification. You should mention them in your assessment.</p>
  {{/unless}}
  <ul class="q-and-a">
    {{#each WRAGAnswers}}
    <li>
      <p class="warn amber">Important</p>
      <p><strong>Question:</strong> {{question}}</p>
      <p><strong>Your answer:</strong> {{answer}}</p>
      <p class="right no-print"><a href="javascript:void(0)" role="button" data-action="change" data-question="question-{{sluggify question}}">Change your answer?</a></p>
    </li>
    {{/each}}
  </ul>
  {{/if}} {{#unless WRAGAnswers}} {{#unless supportAnswers}}
  <p><strong>So far, you haven&#x2019;t provided any answers which you need to remember to mention in your real assessment</strong>. <strong><a href="javascript: void(0)" data-action="start-or-resume">Try some questions <span aria-hidden="true">&#x2192;</span></a></strong></p>
  {{/unless}} {{/unless}}
  </ul>
